<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Le Mie Ricerche - Mercedes Scanner</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            background: white;
            border-radius: 15px;
            padding: 25px 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }

        .header-left {
            flex: 1;
        }

        .header-left h1 {
            color: #667eea;
            font-size: 28px;
            margin-bottom: 5px;
        }

        .user-email {
            color: #666;
            font-size: 16px;
        }

        .header-right {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        .btn {
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-back {
            background: #6c757d;
            color: white;
        }

        .btn-back:hover {
            background: #5a6268;
        }

        .content {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .page-title {
            color: #333;
            font-size: 24px;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 2px solid #e0e0e0;
        }

        .tracking-list {
            display: grid;
            gap: 20px;
        }

        .tracking-card {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 25px;
            border-left: 5px solid #667eea;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .tracking-header {
            display: flex;
            justify-content: space-between;
            align-items: start;
            margin-bottom: 15px;
            gap: 15px;
            flex-wrap: wrap;
        }

        .tracking-info h3 {
            color: #333;
            margin-bottom: 8px;
            font-size: 20px;
        }

        .tracking-email {
            color: #667eea;
            font-weight: 600;
            margin-bottom: 5px;
        }

        .tracking-search {
            color: #666;
            font-style: italic;
        }

        .tracking-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 15px;
        }

        .detail-item {
            display: flex;
            flex-direction: column;
        }

        .detail-label {
            font-size: 12px;
            color: #666;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .detail-value {
            color: #333;
            font-size: 14px;
        }

        .btn-delete {
            background: #dc3545;
            color: white;
            padding: 10px 20px;
            font-size: 14px;
            flex-shrink: 0;
        }

        .btn-delete:hover {
            background: #c82333;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #666;
        }

        .empty-state .icon {
            font-size: 80px;
            margin-bottom: 20px;
        }

        .empty-state h3 {
            font-size: 24px;
            margin-bottom: 10px;
            color: #333;
        }

        .banner {
            position: fixed;
            top: 20px;
            right: 20px;
            padding: 15px 25px;
            border-radius: 10px;
            font-weight: 600;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3);
            z-index: 1000;
            animation: slideInRight 0.3s ease-out;
            display: none;
        }

        .banner.success {
            background: #28a745;
            color: white;
        }

        .banner.error {
            background: #dc3545;
            color: white;
        }

        .banner.warning {
            background: #ffc107;
            color: #333;
        }

        @keyframes slideInRight {
            from {
                transform: translateX(400px);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        .confirm-banner {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            z-index: 2000;
            display: none;
            max-width: 400px;
            width: 90%;
        }

        .confirm-banner h3 {
            color: #333;
            margin-bottom: 15px;
            font-size: 20px;
        }

        .confirm-banner p {
            color: #666;
            margin-bottom: 25px;
            line-height: 1.6;
        }

        .confirm-buttons {
            display: flex;
            gap: 10px;
            justify-content: flex-end;
        }

        .btn-confirm {
            background: #dc3545;
            color: white;
        }

        .btn-cancel {
            background: #6c757d;
            color: white;
        }

        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            z-index: 1500;
            display: none;
        }

        .loading {
            text-align: center;
            padding: 40px;
            color: #666;
        }

        @media (max-width: 768px) {
            .header {
                text-align: center;
            }
            
            .header-left,
            .header-right {
                width: 100%;
                justify-content: center;
            }

            .tracking-details {
                grid-template-columns: 1fr;
            }

            .content {
                padding: 20px;
            }

            .banner {
                top: 10px;
                right: 10px;
                left: 10px;
                text-align: center;
            }

            .tracking-header {
                flex-direction: column;
            }

            .btn-delete {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div id="banner" class="banner"></div>
    <div id="overlay" class="overlay"></div>
    <div id="confirmBanner" class="confirm-banner">
        <h3>⚠️ Conferma Eliminazione</h3>
        <p id="confirmMessage">Sei sicuro di voler eliminare questa ricerca? Questa azione non può essere annullata.</p>
        <div class="confirm-buttons">
            <button class="btn btn-cancel" onclick="hideConfirmBanner()">Annulla</button>
            <button class="btn btn-confirm" onclick="confirmDelete()">Elimina</button>
        </div>
    </div>

    <div class="container">
        <div class="header">
            <div class="header-left">
                <h1>📋 Le Mie Ricerche</h1>
                <p class="user-email" id="userEmail">Caricamento...</p>
            </div>
            <div class="header-right">
                <a href="/home" class="btn btn-back">
                    ← Torna alla Home
                </a>
            </div>
        </div>

        <div class="content">
            <h2 class="page-title">Gestisci le tue ricerche attive</h2>
            <div id="trackingList" class="tracking-list">
                <div class="loading">
                    <p>Caricamento ricerche in corso...</p>
                </div>
            </div>
        </div>
    </div>

    <script>
        const userEmail = localStorage.getItem('userEmail');
        let trackingToDelete = null;

        if (!userEmail) {
            window.location.href = '/login';
        } else {
            document.getElementById('userEmail').textContent = userEmail;
            loadTrackingRequests();
        }

        function showBanner(message, type = 'success') {
            const banner = document.getElementById('banner');
            banner.textContent = message;
            banner.className = `banner ${type}`;
            banner.style.display = 'block';
            
            setTimeout(() => {
                banner.style.display = 'none';
            }, 3000);
        }

        function showConfirmBanner(trackingId, searchValue) {
            trackingToDelete = trackingId;
            const message = searchValue 
                ? `Vuoi eliminare la ricerca "${searchValue}"?`
                : 'Sei sicuro di voler eliminare questa ricerca?';
            
            document.getElementById('confirmMessage').textContent = message;
            document.getElementById('overlay').style.display = 'block';
            document.getElementById('confirmBanner').style.display = 'block';
        }

        function hideConfirmBanner() {
            trackingToDelete = null;
            document.getElementById('overlay').style.display = 'none';
            document.getElementById('confirmBanner').style.display = 'none';
        }

        async function confirmDelete() {
            if (!trackingToDelete) return;

            try {
                const response = await fetch(`/tracking-requests/${trackingToDelete}`, {
                    method: 'DELETE'
                });

                if (response.ok) {
                    showBanner('Ricerca eliminata con successo!', 'success');
                    hideConfirmBanner();
                    loadTrackingRequests();
                } else {
                    showBanner('Errore durante l\'eliminazione', 'error');
                }
            } catch (error) {
                console.error('Errore:', error);
                showBanner('Errore di connessione', 'error');
            }
        }

        async function loadTrackingRequests() {
            const listContainer = document.getElementById('trackingList');

            try {
                const response = await fetch(`/tracking-requests/by-email/${encodeURIComponent(userEmail)}`);

                if (!response.ok) {
                    throw new Error('Errore nel caricamento');
                }

                const data = await response.json();

                if (data.count === 0) {
                    listContainer.innerHTML = `
                        <div class="empty-state">
                            <div class="icon">🔍</div>
                            <h3>Nessuna ricerca attiva</h3>
                            <p>Non hai ancora creato nessuna ricerca. Inizia subito a monitorare le auto Mercedes!</p>
                        </div>
                    `;
                    return;
                }

                listContainer.innerHTML = data.data.map(tracking => {
                    const models = tracking.request.model.join(', ') || 'Tutti';
                    const fuels = tracking.request.fuel.join(', ') || 'Tutti';
                    const priceRange = `€${tracking.request.priceMin.toLocaleString()} - €${tracking.request.priceMax.toLocaleString()}`;
                    const kmRange = `${tracking.request.kilometerMin.toLocaleString()} - ${tracking.request.kilometerMax.toLocaleString()} km`;
                    const yearRange = `${tracking.request.yearMin} - ${tracking.request.yearMax}`;

                    return `
                        <div class="tracking-card">
                            <div class="tracking-header">
                                <div class="tracking-info">
                                    <h3>🚗 Ricerca #${tracking.id}</h3>
                                    ${tracking.searchValue ? `<p class="tracking-search">"${tracking.searchValue}"</p>` : ''}
                                </div>
                                <button class="btn btn-delete" onclick="showConfirmBanner(${tracking.id}, '${tracking.searchValue || ''}')">
                                    🗑️ Elimina
                                </button>
                            </div>
                            <div class="tracking-details">
                                <div class="detail-item">
                                    <span class="detail-label">MODELLI</span>
                                    <span class="detail-value">${models}</span>
                                </div>
                                <div class="detail-item">
                                    <span class="detail-label">CARBURANTE</span>
                                    <span class="detail-value">${fuels}</span>
                                </div>
                                <div class="detail-item">
                                    <span class="detail-label">FASCIA PREZZO</span>
                                    <span class="detail-value">${priceRange}</span>
                                </div>
                                <div class="detail-item">
                                    <span class="detail-label">CHILOMETRI</span>
                                    <span class="detail-value">${kmRange}</span>
                                </div>
                                <div class="detail-item">
                                    <span class="detail-label">ANNO</span>
                                    <span class="detail-value">${yearRange}</span>
                                </div>
                                <div class="detail-item">
                                    <span class="detail-label">ORDINAMENTO</span>
                                    <span class="detail-value">${tracking.request.orderBy} ${tracking.request.orderDir}</span>
                                </div>
                            </div>
                        </div>
                    `;
                }).join('');

            } catch (error) {
                console.error('Errore nel caricamento:', error);
                listContainer.innerHTML = `
                    <div class="empty-state">
                        <div class="icon">❌</div>
                        <h3>Errore nel caricamento</h3>
                        <p>Si è verificato un errore. Riprova più tardi.</p>
                    </div>
                `;
                showBanner('Errore nel caricamento delle ricerche', 'error');
            }
        }
    </script>
</body>
</html>
